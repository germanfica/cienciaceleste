{
  "name": "convertor",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node ./dist/index.js",
    "clean": "tsc --build --clean",
    "convert": "npm run clean && npm run build && node ./dist/index.js --in \"../\" --out \"./markdown\" --pattern \"detallerollo.php-id=*\\\\&pagina=*.htm\" --images true --concurrency 8",
    "dedup": "npm run clean && npm run build && node ./dist/clean-dedup.js --src ./markdown --dest ./complete --duplicates ./duplicates --commit",
    "check-sequence": "npm run clean && npm run build && node ./dist/check-sequence.js --dir ./complete --checkPages true --pageStartAt 1",
    "rename:id": "npm run clean && npm run build && node ./dist/rename-to-id.js --dir ./complete --commit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.2.1",
    "@types/yargs": "^17.0.33",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "fast-glob": "^3.3.3",
    "iconv-lite": "^0.6.3",
    "yargs": "^18.0.0"
  }
}
