{
  "name": "tools",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Internal TypeScript tools for converting HTML documents into Markdown and JSON, deduplicating, validating sequences, and generating structured indexes.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "clean": "tsc --build --clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^22.15.0",
    "@types/yargs": "^17.0.33",
    "fs-extra": "^11.3.1",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "fast-glob": "^3.3.3",
    "iconv-lite": "^0.6.3",
    "yargs": "^18.0.0"
  }
}
